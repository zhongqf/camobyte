/%p= t(user.avatar?? '.we_have_this_avatar_html' : '.gravatar_in_use_html', :gravatar => gravatar_url)
/%p= t('.upload_here_or_at_gravatar_html', :gravatar => gravatar_url)
/%p
/  = f.label :avatar, t('.field')
/  = f.file_field :avatar
/- if user.avatar?
/  %p
/    = f.label :avatar_destroy, t('.destroy')
/    = f.check_box :avatar_destroy
/
/.your_avatars
/  = image_tag avatar_or_gravatar(user, :profile)
/  = image_tag avatar_or_gravatar(user, :thumb)
/  = image_tag avatar_or_gravatar(user, :micro)



= f.simple_fields_for :profile do |p|
  = p.input :avatar
  
  - if p.object.avatar?
    %p
      = p.label :avatar_destroy, t('.destroy')
      = p.check_box :avatar_destroy

.your_avatars
    
  = image_tag user.profile.avatar_url(:profile)
  = image_tag user.profile.avatar_url(:thumb)
  = image_tag user.profile.avatar_url(:micro)
  
    